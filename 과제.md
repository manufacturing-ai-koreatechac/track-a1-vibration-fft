# Track A-1: 진동FFT 고장진단 — 실습 과제

> 실습 노트북을 실행한 후, 동일한 노트북 안에 새 셀을 추가하여 과제를 완성하세요.
> **새 라이브러리 설치 불필요 | 과제당 소요 시간: 약 15~20분**

---

## 📒 노트북 01 — 진동 데이터 분석

**과제 A (필수)**
실습에서 `threshold=3` 으로 3-sigma 이상 탐지를 수행했습니다.
`threshold=2`로 바꿔 실행하고, 이상 포인트 수가 얼마나 달라지는지 출력하세요.

```python
anomalies_2 = detect_anomalies(signal, threshold=2)
print(f"threshold=2: 이상 포인트 수 = {anomalies_2.sum()}")
print(f"threshold=3: 이상 포인트 수 = {anomalies_3.sum()}")
```

**과제 B (필수)**
실습에서 X축 진동 시계열을 그렸습니다. 같은 코드로 **Z축** 진동 시계열을 그리세요.

**과제 C (심화·선택)**
X·Y·Z 세 축의 RMS를 각각 계산하여 어느 축이 가장 높은지 출력하세요.

```python
for col in vibration_cols:
    rms = np.sqrt(np.mean(df[col]**2))
    print(f"{col} RMS = {rms:.4f}")
```

---

## 📒 노트북 02 — FFT 주파수 분석

**과제 A (필수)**
실습에서 그린 FFT 스펙트럼에 `plt.xlim(0, 500)`을 추가하여 0~500Hz 범위로 확대하세요.

```python
plt.xlim(0, 500)
plt.title('FFT 스펙트럼 (0~500Hz)')
plt.show()
```

**과제 B (필수)**
FFT 결과에서 진폭이 가장 큰 주파수를 찾아 출력하세요.

```python
peak_idx = np.argmax(magnitude)
print(f"최대 진폭 주파수: {frequencies[peak_idx]:.1f} Hz")
print(f"최대 진폭: {magnitude[peak_idx]:.2f}")
```

**과제 C (심화·선택)**
다른 신호 구간(예: 후반부 1000개 샘플)으로 FFT를 다시 그려 비교하세요.

---

## 📒 노트북 03 — 이상 탐지 모델

**과제 A (필수)**
`IsolationForest(contamination=0.1)`을 `contamination=0.05`로 바꾸고,
이상으로 탐지된 샘플 수를 비교해 출력하세요.

```python
iso_05 = IsolationForest(contamination=0.05, random_state=42)
pred_05 = iso_05.fit_predict(features)
print(f"contamination=0.05 → 이상 = {(pred_05==-1).sum()}개")
print(f"contamination=0.10 → 이상 = {(pred_10==-1).sum()}개")
```

**과제 B (필수)**
Health Index 그래프에서 70 이하로 처음 떨어지는 인덱스를 출력하세요.

```python
below = np.where(health_index < 70)[0]
print(f"경고 시작 인덱스: {below[0] if len(below) > 0 else '없음'}")
```

**과제 C (심화·선택)**
`n_estimators`를 50·100·200으로 바꾸며 이상 탐지 수를 비교하세요.

---

## 📌 제출 방법

- 기존 노트북에 과제 셀을 추가하고 전체 실행 후 `.ipynb` 파일 제출
- **제출 기준**: 과제 A·B 실행 완료 + 결과(출력 또는 그래프) 확인 + 관찰 주석 1줄
